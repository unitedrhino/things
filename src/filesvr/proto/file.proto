syntax = "proto3";
option go_package = "pb/file";

package file;


service File {
  rpc Upload(stream  UploadReq) returns(UploadResp);
}

message UploadReq{
  string objectName = 1;
  int64 bucket = 2;
}

message UploadResp{
  int64 refreshAfter = 3;
}
